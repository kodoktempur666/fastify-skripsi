services:
  app:
    build: ./app
    container_name: fastify_app
    ports:
      - "3005:3005"
    environment:
      PORT: 3005
      DB_HOST: db
      DB_USER: postgres
      DB_PASSWORD: postgres
      DB_NAME: mydb
    networks:
      - monitoring

networks:
  monitoring:
    external: true
